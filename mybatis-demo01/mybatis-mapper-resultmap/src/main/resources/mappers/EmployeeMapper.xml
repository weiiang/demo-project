<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wq.mybatis.dao.EmployeeMapper">
    
   <resultMap id="defaultMap" type="Employee">
        <constructor>
            <arg column="hireDate" javaType="java.util.Date" />
        </constructor>
   </resultMap>

    <select id="findAll" resultType="Employee">
        select * from emp2
    </select>


    <select id="findAll2" resultMap="defaultMap">
        select * from emp2
    </select>
</mapper>